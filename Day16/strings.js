// longitude.indexOf("l") method returns the first occurence of that character
console.log("ALOHA".indexOf("L"));

// "longitude".includes("long") method returns true if a string is present inside of it\
console.log("ALOHA".includes("LOHA"));

// "longitude".startsWith("tude") will return false 
console.log("ALOHA".startsWith("HA"));

// "LONGITUDE.toLowerCase() lowercases all the letters nd returns the string
console.log("ALOHA!".toLowerCase());
